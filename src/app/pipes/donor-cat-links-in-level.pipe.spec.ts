import { DonorCatLinksInLevelPipe } from './donor-cat-links-in-level.pipe';

describe('DonorCatLinksInLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new DonorCatLinksInLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
