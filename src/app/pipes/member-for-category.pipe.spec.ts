import { MemberForCategoryPipe } from './member-for-category.pipe';

describe('MemberForCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberForCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
