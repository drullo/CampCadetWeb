import { LinkForCategoryPipe } from './link-for-category.pipe';

describe('LinkForCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkForCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
